form(method='POST')
    p Name:
        input(type='text', name='name')
    p Job:
        input(type='text', name='job')
    p: button Add

if people.length > 0
    ul
        each person in people
            li
                h2 person.name + '(' +person.job+ ')'
else
    p No people